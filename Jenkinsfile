pipeline {
    agent any

    stages {
        // stage('git clone') {
        //     steps {
        //         sh "git clone https://github.com/AnimelaAsif/Jenkins_monitoring.git"
        //     }
        // }
        stage("Run python file"){
            steps {
                sh "ls"
                sh "python3 app1.py"
            }
        }
    }
}
